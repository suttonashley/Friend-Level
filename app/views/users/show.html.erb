<h1><%= @user.username %></h1>


<h2><%= @user.username %>'s current friends:</h2>
<% @user.friends.each do |friend| %>
<p>
  <% if friend.friendships.where(status: 'accepted', friend_id: @user.id) != [] %>
  @<%= friend.username %>
  <% end %>
</p>
<% end %>
