<%= form_for(@task) do |f| %>

<%= f.label :title %>:
<%= f.text_field :title %>

<%= f.label :details %>:
<%= f.text_field :details %>

<%= f.label :due_date%>:
<%= f.datetime_field :due_date %>

<%= f.label :points %>:
<%= f.number_field :points %>

<%= f.submit "Submit" %>

<% end %>
